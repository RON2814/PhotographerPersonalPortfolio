# PhotographerPersonalPortfolio

# Check Python ver
py --v

# Add virtual environment
py -m venv .venv

# Activate virtual environment
.venv/Scripts/activate

# Deac virtual envi
deactivate

# Python Django installation inside virtual envi
py -m pip install Django

# Update pip version
py -m pip install -U pip
